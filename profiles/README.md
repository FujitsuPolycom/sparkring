# Deployment profiles

Choose a profile in the generated [project table](../README.md#profiles).
`catalog.json` indexes definitions; it does not repeat defaults. Each definition
selects one configuration, release and primary guide. See the
[configuration contract](../docs/development/configuration.md).
