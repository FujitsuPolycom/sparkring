# GLM-5.3-Flash DCP1 without SparkCache

Use the [four-Spark quickstart](../glm53-flash-spark-tp4-dcp1-sparkcache/README.md#3-discover-and-plan-dcp1)
and select `tp4-dcp1`. DCP1 is the default; DCP4 is an alternative.

Keep the published image receipt when choosing the cache-disabled selection.

See [profile.json](profile.json) for the exact release and evidence scope.
